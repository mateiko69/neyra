"""Domain layer (entities and business rules).

The current codebase uses SQLAlchemy models as the primary entities. Over time,
pure domain models can be introduced without changing the interface layer.
"""

